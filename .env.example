# Base URL of omnirehuma_backend. Must match the PORT in the backend .env.
VITE_API_BASE_URL=http://localhost:5001

# Only needed if API_KEY is set in the backend .env.
# Note: anything with a VITE_ prefix ends up in the browser bundle.
VITE_API_KEY=
